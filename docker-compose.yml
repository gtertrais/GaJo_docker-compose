# ./docker-compose/docker-compose.yml
version: '3'
services:
  frontend:
    image: "gtertrais/front"
    networks:
      - sub-etha
  backend:
    image: "gtertrais/server"
    networks:
      - sub-etha
  proxy:
    image: "gtertrais/proxy"
    networks:
      - sub-etha
    ports:
      - "80:80"
networks:
  sub-etha: